export const PLATFORM_TYPES = Object.freeze({
  DEFAULT: "DEFAULT",
  FINISH: "FINISH",
  HOLE: "HOLE",
});

export const PLATFORM_COLORS = Object.freeze({
  DEFAULT: 0x0ffff0,
  FINISH: 0x20a030,
});
